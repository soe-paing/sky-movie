/** @type {import('next').NextConfig} */
const nextConfig = {
    typescript: {
        ignoreBuildError: true,
    },
    eslint: {
        ignoreBuildError: true,
    }
};

export default nextConfig;
